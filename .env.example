# Copy to .env and fill in real values. Never commit .env.

# Self-hosted cobalt instance (see cobalt/docker-compose.yml).
# api.cobalt.tools cannot be used here — its docs prohibit third-party use.
COBALT_API_URL=https://your-tunnel-url.trycloudflare.com/

GROQ_API_KEY=gsk_xxxxx
OPENAI_API_KEY=sk-xxxxx
