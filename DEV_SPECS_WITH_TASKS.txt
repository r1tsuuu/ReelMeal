# ⚠️ SUPERSEDED — read REELMEAL_CODING_CONTEXT.md instead.

This file (DEV_SPECS_WITH_TASKS.txt) is OUT OF DATE and is NO LONGER THE SPEC.
It still uses the old "reel-to-recipe" naming, the 300s timeout, and the dropped
model list — do not build from it.

The canonical build context is **REELMEAL_CODING_CONTEXT.md** in this same directory.
That file is the single source of truth. Build from it.
